// 基盤雛形。タイマー実装は docs/backlog.md の Next を参照（ADR 0003 / design 準拠）。
export function App() {
  return (
    <main>
      <h1>Pomodoro Timer</h1>
      <p>基盤セットアップ完了。次はタイマー状態機械（useTimer）を実装します。</p>
    </main>
  );
}

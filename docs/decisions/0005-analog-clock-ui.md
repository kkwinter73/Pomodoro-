---
id: 0005
title: 表示は白黒ミニマルなアナログ時計（SVG・数字非表示）にする
status: accepted
date: 2026-06-19
links:
  - docs/design/pomodoro-design.md
---

## 決定
タイマー表示をデジタル mm:ss から、白黒・ローマ数字・細枠の**アナログ時計（SVG）**に刷新する。残りは1本の針で表し満了で一周（経過割合 = `useTimer.progress`）。視覚的な数字は出さず、残り時間は `role="timer"` の `aria-label` でのみ公開する。

## 背景 / 根拠
- ユーザー要望（参照画像）: 白黒・余計な情報を載せない・文字少なく・枠も少なく。
- ロジックと表示が分離済み（#1）のため、刷新は `src/ui/` に閉じる。`useTimer` に `totalMs`/`progress` を追加して描画に必要な経過割合だけ供給。
- アクセシビリティと自動テストのため、数字は視覚非表示でも `aria-label` で残り時間を保持する（純粋に針だけにすると残り秒が読めない問題への対処）。
- 設定パネルは既定で折りたたみ（「設定」トグル）し、主画面を時計＋操作だけに保つ（“余計な情報を載せない”）。

## 含意 / 不採用
- 1針一周＋ローマ数字を採用。さらに分目盛りを追加（高級時計の精密感）。
- 完了ポモドーロ数の常時表示は廃止（最小表示を優先）。必要になれば非テキストのドット表示等で再検討。

## 視覚コンセプト: 高級時計（ラグジュアリー）
- ユーザー要望「プロのデザイナーっぽく」を受け、Georgia 等のシステム書体由来の量産感を排除。
- 書体: 表示は Web フォント **Cormorant Garamond**（数字・フェーズ）、UI は **Inter**（トラッキングした小さな大文字ラベル）。Google Fonts 読み込み。
- 配色: **クリスプな白×黒（クールなニュートラル）**。白文字盤 × 近黒 #0d0d0d、淡いクール白の背景。
  （当初は暖色クリームだったが「Claude Code っぽい＝温かいベージュ」という指摘を受け、編集的・ギャラリー風の白黒へ変更）
- 細部: テーパー針＋カウンターウェイト、分目盛り、二重ベゼル、柔らかいドロップシャドウ、振り子。
- 余白/階層: 大胆な余白とトラッキングで“編集的”な静けさ。設定はトグルで折りたたみ。
